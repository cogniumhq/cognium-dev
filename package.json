{
  "name": "cognium-dev-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Cognium-dev: Static Application Security Testing platform",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/circle-ir && npm run build -w packages/project-profile-detect && npm run build -w packages/cli && npm run build -w packages/mcp-server",
    "build:all": "npm run build:all -w packages/circle-ir && npm run build -w packages/project-profile-detect && npm run build -w packages/cli && npm run build -w packages/mcp-server",
    "test": "npm run test --workspaces",
    "test:coverage": "npm run test:coverage --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "clean": "npm run clean --workspaces",
    "dogfood": "npm run dogfood -w packages/cli",
    "release": "bash release.sh",
    "lint": "eslint .",
    "test:scripts": "node --test 'scripts/*.test.mjs'"
  },
  "author": {
    "name": "Cognium Labs",
    "url": "https://cognium.dev"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cogniumhq/cognium-dev.git"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "typescript-eslint": "^8.18.0"
  }
}
